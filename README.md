This is Content Management System sometimes called CMS was built to manage the company's employees using node, inquirer, and MySQL.

To start the application, you will enter "node finalserver" into the terminal. 

Once the server is running several options will appear to make the necessary changes to the employee. The choices include viewing, add, update once done you will use the exit selection to exit the system.